{% extends 'base.html.twig' %}

{% block title %}Edition d'une annonce{% endblock %}

{% block body %}

    <div class="container">
        <h1>Modifictaion de l'annoce :   {{ ad.title }}</h1>
        {{ form_start(form) }}

        {{ form_widget(form) }}

        <button type="submit" class="btn btn-primary"> Enregister les modification </button>

        {{ form_end(form) }}
    </div>


{% endblock %}
